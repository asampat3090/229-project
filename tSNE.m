function [reducedVectorSetY,updatedReducedVectorSetY] = tSNE(initialDataX,initialDataY)
    % initialDataX is a column vector with each row being an x for each
    % example
    % initialDataY is a matrix with each row for each example and each
    % column representing one of the y features.
    
end